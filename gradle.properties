# Project
VERSION_MAJOR = 1
VERSION_MINOR = 2
VERSION_PATCH = 1

ANDROID_VERSION_MINIMAL = 21
ANDROID_VERSION_TARGET = 28

# Signing
KEYSTORE_PATH = ../signing/keystore.jks
KEYSTORE_PASSWORD_FILE_PATH = ../signing/keystore_password.txt
KEYSTORE_ALIAS_FILE_PATH = ../signing/keystore_alias.txt
KEYSTORE_ALIAS_PASSWORD_FILE_PATH = ../signing/keystore_alias_password.txt

# Gradle
org.gradle.jvmargs = -Xmx1536m

# AndroidX
android.useAndroidX = true
android.enableJetifier = true